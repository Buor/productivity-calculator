import {Box} from "@chakra-ui/react";
import React from "react";

interface IProps {
    month: Date
}

export const CalendarDays: React.FC<IProps> = ({month}) => {



    return <Box d={'flex'} flexWrap={'wrap'}>

    </Box>
}