import {extendTheme} from "@chakra-ui/react"

export const customTheme = extendTheme({
    colors: {
        custom: {
            dark: 'rgb(18,18,20)',
            medium: 'rgb(37,36,39)'
        }
    }
})